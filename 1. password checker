# to check how many letters in a password

username = "username:"
password = "password:"

print(type(username))

length = len(password)

print(f'hello {username} your password {length * "*"} is {length}')
# print("hello " + username + " your password " + length * "*" + " is " + str(length))

print(f'length')
